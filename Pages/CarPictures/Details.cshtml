@page
@model Fribergs_rentals_2.Pages.CarPictures.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CarPicture</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CarPicture.CarPicURL)
        </dt>
        <dd class="col-sm-10">
            <img class="imgBig" src="@Model.CarPicture.CarPicURL" alt="Bild på bil"
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.CarPicture.CarPictureId" asp-route-carId="@Model.CarPicture.Car.CarId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
